# Core runtime. The analysis deliberately depends only on the scientific-Python
# core: every econometric estimator used in this project (GJR-GARCH, Hansen skewed-t,
# GPD/peaks-over-threshold, HAR, Newey-West HAC, Yang-Zhang) is implemented in
# src/svcarry/econometrics and unit-tested, rather than imported from a library.
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
requests>=2.28
PyYAML>=6.0

# Optional. If present, the test suite cross-checks the hand-rolled estimators
# against these reference implementations; if absent, those tests skip.
# statsmodels>=0.14
# arch>=6.0
# pytest>=7.0
